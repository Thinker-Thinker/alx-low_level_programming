#include "main.h"
/**
* print_rev - Entry point
*@s: pointer
* Return: Always 0 (Success)
*/
void print_rev(char *s);
{
	int i;
	int rev

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	for (; s[i] != '\0'; i--)
	{
		_putchar(s[i]);
	}
}

